<main class="container">
  <h1 class="rateHead"> Welcome to your favourites profile!</h1>
  <p class ="profileBio">Explore the home page, if anything catches your eye you can store it right here using the favourites button.</p>
  <div class="columns is-multiline">
    <% user.favouriteList.forEach(restaurant => { %>
      <div class="column is-half-desktop is-half-tablet">
        <a href="/restaurants/<%= restaurant._id %>">
          <div class="card">
            <div class="card-image">
              <figure class="image">
                <img src="<%= restaurant.image1 %>" alt="<%= restaurant.name%>">
              </figure>
            </div>
            <div class="card-content">
              <h3 class="title"><%= restaurant.name %></h3>
              <h4 class="subtitle"><%= restaurant.cuisine %></h4>
            </div>
          </div>
        </a>
      </div>
    <% }) %>
  </div>
</main>
